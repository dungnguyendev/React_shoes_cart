/**
 * React CONTEX là chuyền xuyên cấp giữa các components
 * và ta có thể bọc nhiều context 
 * jsx là 1 cây react element
 */
import { createContext } from "react";
export const DataConText = createContext();